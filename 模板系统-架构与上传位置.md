# 模板系统 — 架构与"文件上传位置"权威说明

> 改模板相关代码、加新模板、迁移素材之前**先读这份**。把"哪个文件放哪、哪段配置在哪"一次钉死,以后不迷路。
>
> 两个仓库:`ai-canvas`(Tauri 桌面端)+ `ai-canvas-server`(Spring 后端, 部署 `101.37.80.236`, 库 `aicat`)。

---

## 一句话架构(最终版)

**模板 = 定义(放画布 server)+ 图片/视频文件(放极境 NAS,内容哈希命名)。桌面端拉定义 → Rust 把图下到本地文件夹 → 渲染本地文件(不用浏览器缓存)。**

```
桌面端 ──GET /api/templates──> 画布 server(aicat.template)──> 定义 JSON(图=极境 NAS 哈希 URL)
   │
   ├─ Rust sync_template_assets:把图下到 {data_dir}/template-assets/<base>.<sha16>.<ext>
   │     (从极境 NAS aicanvas-static/templates/ 下;文件名含哈希→存在即跳过=下载一次;清孤儿)
   │
   └─ <img src="asset://localhost/.../template-assets/white-bg.<sha16>.jpg">  本地文件,离线、零重复流量
```

**为什么本地下载而非浏览器缓存**:下载一次是硬保证(文件夹永不淘汰),可见可整理可清旧版本,跟 ffmpeg 下载器同一套机制,走 Rust 出站合规。**内容哈希根治 stale**:换图=内容变=哈希变=URL 变 → 客户端拉到新 URL 自动下新图、prune 旧的;同一 URL 内容永不变。

---

## 两个"位置",钉死

| 东西 | **正确位置** | 怎么到那 | ❌ 不要 |
|---|---|---|---|
| 模板**图片/视频文件** | **极境 NAS** `192.168.31.244:/mnt/nas/ec_system/aicanvas-static/templates/<rel>`,**内容哈希命名** `<base>.<sha16>.<ext>`,nginx 静态 serve,公网 `https://ai.snoworangekeji.cn/aicanvas-static/templates/<rel>`,宽带快、**非 COS**。桌面端再由 Rust `sync_template_assets` 下到本地 `{data_dir}/template-assets/`(**非浏览器缓存**) | `upload-template-assets.py` 按 `templates-assets-manifest.json` 传哈希名 + prune NAS 旧文件 | ❌ 腾讯 COS(流量贵)<br>❌ 极境 `/v1/files/upload`(返 COS 直链)<br>❌ 画布 server 自 serve(宽带不如极境)<br>❌ 浏览器缓存(会淘汰、不可控)<br>❌ 固定文件名(换图会 stale)<br>❌ `import` 进 bundle |
| 模板**定义**(id/卡片/连线/prompt/图 URL) | **画布 server** `101.37.80.236` MySQL `aicat.template`.`definition` JSON | `seed-templates.mts` 产 JSON → `seed-templates-db.py` 写库 | ❌ 硬编码桌面端 `workflows.ts`(改一次要发版) |
| (沿用)哪个 tier 能用哪些模板 | `aicat.tier_def.features.templates` | 不新建 | — |

**安装包**:图不进包、定义不进包 ⇒ 安装包不随模板数量增长(视频模板再多也不涨)。桌面端只随包带一份 `src/config/templatesFallback.json`(纯文本相对极小,离线兜底)。

---

## 加一个新模板的完整步骤(以后照这个做)

1. `ai-canvas/src/config/workflows.ts` 写模板定义,图 `import "@/assets/templates/<rel>"`(图放 `src/assets/templates/<rel>`)。
2. `cd ai-canvas && npx vite-node scripts/seed-templates.mts` —— **算内容哈希**、图引用改写成 `<base>.<sha16>` 极境 NAS URL,产 seed/fallback JSON + `templates-assets-manifest.json`。
3. `$env:AICAT_SSH_PASSWORD="..."; python scripts/upload-template-assets.py` —— 按 manifest 传哈希名到**极境 NAS**(192.168.31.244)+ prune NAS 旧文件。
4. `python scripts/seed-templates-db.py` —— upsert 进**画布 server**(101.37.80.236)`aicat.template`(**只刷新 cover_url+definition,保留 is_active/排序等 admin 运营状态**)。
5. 完事。**零桌面发版、零重签、安装包不涨、零 COS 流量。** 视频模板同理(.mp4 放 templates/ 下)。

**更新一张图**:替换 `src/assets/templates/<rel>` 的文件 → 重跑 2/3/4。哈希自动变 → 新 URL → 客户端下次拉定义自动下新图、删旧图。**无需任何手动清缓存**。
**改名/上下架/排序**:走 admin「模板管理」页(re-seed 不覆盖这些)。

---

## 实现要点

**画布 server(`ai-canvas-server`,只管定义,不serve图)**
- 表 `aicat.template`(`schema.sql`):`id VARCHAR(64) PK`(业务键,对齐 `features.templates`)、`definition JSON`(整个 WorkflowTemplate,图为极境 NAS 绝对 URL)、`min_app_version`、`sort/is_active`…
- `GET /api/templates`(公开)→ `Result<List<WorkflowTemplate>>` 全 active,`?appVersion=` 版本守卫。`/api/admin/template/**`(JWT)增删改。
- 文件:`controller/TemplateController.java` `TemplateAdminController.java` `service/TemplateService.java` `entity/Template.java` `mapper/TemplateMapper.java` `common/ErrorCode.java` `db/schema.sql`。`WebMvcConfig` **不动**(画布 server 不 serve 图)。
- 构建 `mvn clean package`→`target/ai-canvas-server-0.1.0.jar`;部署 `D:\Project\AI无限画布\deploy-template-feature.py`(备份+apply schema+换 JAR+重启+验活,可回滚)。

**桌面端(`ai-canvas`)**
- `src/platform/templates.api.ts`:`httpJson`(Rust 桥,不直接 fetch)拉 `GET /api/templates`。
- `src-tauri/src/commands/template_assets.rs` 的 `sync_template_assets(urls)`(已注册 invoke_handler):把图下到 `{data_dir}/template-assets/<哈希名>`(泛化 ffmpeg 下载器:并发6、存在跳过、prune 孤儿、Content-Length 校验、.partial 原子改名),返回 url→本地 rel。前端绑定 `src/platform/templateAssets.ts`。
- `src/stores/templateStore.ts`:缓存优先 + `templatesFallback.json` 兜底 + `getVersion()` 版本守卫;`load()` 拉定义 → `await initMediaService()` → 调 `syncTemplateAssets` 下图 → 用 `lib/media.ts::getDisplayUrl(rel)` 把图换成本地 `asset://localhost/...`(非 Tauri/dev 环境 Rust 不可用 → 回退远程 URL,getDisplayUrl 对 https 透传)。
- 消费点 `WorkflowGrid`/`NewProjectDialog`/`ContextMenu` 改用 `useTemplateStore`;`WorkflowGrid` 挂载触发 `load()`。`workflows.ts` 已无 app 引用 → Vite tree-shake → 图不进 dist(只当种子源)。
- ⚠ Rust 命令编译进 app 二进制 → **下次 `tauri build`/发版才生效**(纯前端 `vite build` 不含 Rust)。

---

## 凭据 / 网络

- **极境 NAS**(图):`root@192.168.31.244` / `AImao123456!`(内网,用户机直连)。`upload-template-assets.py` 从 `AICAT_SSH_PASSWORD` 读。
- **画布 server**(定义):`root@101.37.80.236` / `AImao123456!`(见 `deploy.py`)。脚本从 `AICAT_SSH_PASSWORD` 读。
- 凭据**绝不写进任何提交文件**;桌面端 base `server_base_url`(默认 `http://101.37.80.236`)。

---

## 历史:踩过的弯路(别再走)

1. **画布 server 自 serve `/api/template-assets`**:能跑,但极境宽带更快 → 撤回。
2. **极境 `/v1/files/upload`**:能传、永久,但返**腾讯 COS 直链**(`*.myqcloud.com`)→ 客户端走 COS egress(贵)→ 弃用。
3. **极境 NAS `aicanvas-static`(当前)**:图放极境 NAS 磁盘,nginx 静态 serve,宽带快、零 COS。✅
